application/container.rs # ADR-0011 DI root — composition root must know concretes (CpuBridge, RayonCpuPool, DomainSessionPool, ResourceDownloader)
infrastructure::observability # ADR-0011 transversal — log_scrape_error, memory_probe (no domain value, tracing is the stack)
infrastructure::crawler # ADR-0011 deferred — UrlQueue, RobotsFetcher, SitemapParser, extract_links, UrlSource (follow-up slice)
infrastructure::export # ADR-0011 deferred — StateStore, RecordStore, DomainRecords, RawRecord (follow-up slice, replaces broad)
application/asset_download.rs # ADR-0010 inline path absorbed; porting tracked in #994 sub-slice 3 — 1 inline site of crate::adapters::downloader::Downloader::new. Remove after sub-slice 3 ports the Downloader factory call to a domain DownloaderFactory port.
application/crawler/discovery.rs # ADR-0010 inline path absorbed; porting tracked in #994 sub-slice 3 — remaining inline sites absorbed by the infrastructure::crawler / infrastructure::observability broad entries (the infrastructure::downloader sites were repointed to domain::downloader_port in sub-slice 3.B-0). Remove after sub-slice 3 ports the crawler concretes to domain ports.
application/crawler/engine.rs # ADR-0010 inline path absorbed; porting tracked in #994 sub-slice 3 — 3 inline sites of crate::infrastructure::network::session_pool::DomainSessionPool, plus the `use crate::infrastructure::downloader::system_ram_probe::SystemRamProbe` at engine.rs:57 that constructs the default `ram_probe` field in `Engine::new()`. The autoscale loop now reads the injected domain port `RamProbePort` (ADR-0012 sub-slice 3.B-1c) instead of the static `ResourceGovernor::ram_usage_percent()` — porting DONE — but the default-probe construction is still infra-bound (engine.rs is not the composition root, but every test path constructs `Engine::new` without going through `cli`). Move to a domain-side factory (e.g. `RamProbePort::system_default()`) in a follow-up if `cli` becomes the only construction site. Remove after sub-slice 3 ports DomainSessionPool to a domain SessionPool port and the `SystemRamProbe` default is hoisted.
application/crawler_service.rs # ADR-0010 inline path absorbed; porting tracked in #994 sub-slice 3 — remaining inline sites absorbed by the infrastructure::crawler broad entry. Remove after sub-slice 3 ports the crawler concretes to domain ports.
application/elastic_ingestion.rs # ADR-0010 inline path absorbed; porting tracked in #994 sub-slice 3 — 8 non-test inline sites of crate::infrastructure::bridge::CpuBridge; the `use crate::infrastructure::crawler::resource_downloader` line is absorbed by the infrastructure::crawler broad entry. (3 sites of crate::infrastructure::config::AutotuningConfig migrated to crate::domain::config::AutotuningConfig in #994 sub-slice 1.) Remove after sub-slice 3 ports the CpuBridge port to domain::bridge.
application/vault_search.rs # ADR-0010 inline path absorbed; porting tracked in #994 sub-slice 3 — 1 inline site of crate::infrastructure::obsidian::read_vault_notes. Remove after sub-slice 3 ports the Obsidian reader to a domain ObsidianPort.
