application/container.rs # ADR-0011 DI root — PERMANENT, never removed. The composition root must name concretes (CpuBridge, RayonCpuPool, DomainSessionPool, ResourceDownloader, StateStore, WafInspector). Migration that would end this exemption: make cli/ the only construction site; tracked in #994.
infrastructure::observability # ADR-0011 transversal — PERMANENT, never removed. log_scrape_error and memory_probe carry no domain value; tracing is the stack (no OpenTelemetry since #356). Migration that would end this exemption: none planned.
# infrastructure::crawler narrowed from one broad entry to 15 per-symbol entries (#1082, ADR-0012-B §2.2 "narrowed before deletion").
# Measured on 7768f23b: the broad entry absorbed 22 sites across 15 distinct module paths. Issue #1082 cited 9 symbols because it was
# measured on 8dc58c6e, before #1073/#1075/#1077/#1079/#1080/#1081 dropped the per-file entries that used to cover the other 6 paths.
# Each entry cites its removal condition by SYMBOL, never by line number (line cites rot — #1032). A new crate::infrastructure::crawler::*
# path now fails closed instead of being silently absorbed by a broad substring match.
infrastructure::crawler::UrlQueue # ADR-0012-B pre-3.F-cap — UrlQueue (home: url_queue, re-exported at crawler root). Migration: port slice adds domain::crawler_port::UrlQueuePort (trait in domain, concrete stays infra per ADR-0012-B §2.1); repoint UrlQueue sites and delete this entry.
infrastructure::crawler::fetch_url # ADR-0012-B pre-3.F-cap — fetch_url (home: http_client). Migration: port slice adds a fetch trait in domain::crawler_port (wreq concrete stays infra per ADR-0012-B §2.1); repoint and delete this entry.
infrastructure::crawler::extract_links # ADR-0012-B pre-3.F-cap — extract_links (home: link_extractor). Migration: port slice exposes the link-extraction surface via domain::crawler_port (HTML-parser concrete stays infra); repoint and delete this entry.
infrastructure::crawler::parse_sitemap # ADR-0012-B pre-3.F-cap — parse_sitemap (home: sitemap_parser). Migration: moves with the SitemapParser surface to domain::crawler_port; repoint and delete this entry.
infrastructure::crawler::resource_downloader::ResourceDownloader # ADR-0012-B pre-3.F-cap — resource_downloader::ResourceDownloader. Migration: resource-download port slice (domain::asset_downloader_factory precedent) repoints ResourceDownloader sites; delete this entry.
infrastructure::crawler::binary_utils::derive_filename_from_response # ADR-0012-B pre-3.F-cap — binary_utils::derive_filename_from_response. Migration: pure filename helper moves to domain::crawler_port; repoint and delete this entry.
infrastructure::crawler::FsBinaryWriter # ADR-0012-B pre-3.F-cap — FsBinaryWriter via the crawler-root re-export (home: binary_writer). Migration: filesystem writer stays infra behind a domain::crawler_port writer trait; repoint and delete this entry.
infrastructure::export::DomainRecords # ADR-0012-B 3.H pre-step — narrowed from the infrastructure::export broad entry. 2 sites: application/export_factory.rs:32 and application/resume.rs:19 (both brace-group imports). Removal-condition (this symbol): ADR-0012-B §2.3 names DomainRecords for the 3.H move of the record vocabulary into domain::exporter; delete when application imports crate::domain::exporter::DomainRecords. domain::exporter already owns Exporter / ExporterError / ExporterConfig, so only the record types are outstanding.
infrastructure::export::RawRecord # ADR-0012-B 3.H pre-step — narrowed from the infrastructure::export broad entry. 2 sites: application/export_factory.rs:32 and application/resume.rs:19. Removal-condition (this symbol): same 3.H record-vocabulary move — §2.3 names RawRecord explicitly; delete when application imports crate::domain::exporter::RawRecord.
infrastructure::export::RecordStore # ADR-0012-B 3.H pre-step — narrowed from the infrastructure::export broad entry. 3 sites: application/export_factory.rs:32, application/resume.rs:19, and the function-local use at application/resume.rs:120. Removal-condition (this symbol): NOT named by §2.3's 3.H list (which cites ExportState/Exporter/DomainRecords/RawRecord). It is a companion of the same re-export group at infrastructure/export/mod.rs:22, so it rides on the same record-vocabulary move; delete only once DomainRecords and RawRecord are gone AND this type is reached through a domain port.
infrastructure::export::LastError # ADR-0012-B 3.H pre-step — narrowed from the infrastructure::export broad entry. 1 site: application/export_factory.rs:32. Removal-condition (this symbol): same re-export group as RecordStore (infrastructure/export/mod.rs:22) and likewise not named by §2.3; delete with the record-vocabulary move that takes RecordStore.
infrastructure::export::state_store::StateStore # ADR-0012-B 3.H pre-step — narrowed from the infrastructure::export broad entry. 3 sites: application/export_factory.rs:32, the function-local use inside create_state_store at application/export_factory.rs:616, and the inline qualified path in the record_store_bridge signature at application/resume.rs:118. Removal-condition (this symbol): ADR-0012-B §2.3 keeps the state store in infrastructure ("state-store stays infra"), and §5.1 measured 3.K at 0 sites, so 3.K is NOT the port that removes this. Delete only when a real domain state-store port exists — do not delete on the strength of 3.H.
infrastructure::export::StateStore # ADR-0012-B 3.H pre-step — narrowed from the infrastructure::export broad entry. 1 site: application/export_utils.rs:7. Same concrete type as the entry above, reached through the pub use state_store::StateStore re-export at infrastructure/export/mod.rs:23. Removal-condition (this symbol): repoint that single import at the module path (this line then collapses into the state_store::StateStore entry above), or hoist the construction into application/container.rs, which the permanent DI-root entry absorbs.
infrastructure::export::vector_exporter::VectorExporter # ADR-0012-B 3.H pre-step — narrowed from the infrastructure::export broad entry. 1 site: application/export_factory.rs:32, constructed at :545 as Box::new(VectorExporter::new(config)) behind a Box<dyn Exporter>. Removal-condition (this symbol): the trait side needs no work — domain::exporter::Exporter already exists — so delete once the concrete construction is hoisted into application/container.rs, where the permanent DI-root entry absorbs it.
infrastructure::export::jsonl_exporter # ADR-0012-B 3.H pre-step — narrowed from the infrastructure::export broad entry. 1 site: application/export_factory.rs:32, used at :538 as jsonl_exporter::JsonlExporter::new(config). Removal-condition (this symbol): identical to vector_exporter::VectorExporter — hoist the concrete construction into application/container.rs and delete when the module path is no longer named from application/.
