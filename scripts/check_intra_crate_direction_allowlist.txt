application/container.rs # ADR-0011 DI root — PERMANENT, never removed. The composition root must name concretes (CpuBridge, RayonCpuPool, DomainSessionPool, ResourceDownloader, StateStore, WafInspector). Migration that would end this exemption: make cli/ the only construction site; tracked in #994.
infrastructure::observability # ADR-0011 transversal — PERMANENT, never removed. log_scrape_error and memory_probe carry no domain value; tracing is the stack (no OpenTelemetry since #356). Migration that would end this exemption: none planned.
infrastructure::crawler # ADR-0011 deferred — UrlQueue, robots_utils::RobotsFetcher, SitemapParser, SitemapUrl, SitemapError, SitemapConfig, fetch_url, extract_links. Migration: ADR-0012-B narrows this broad entry to per-symbol entries and deletes it ONE MODULE AT A TIME (cap 22 forbids narrowing crawler+export+observability together — see ADR-0012-B 1.2.2). Do not delete before every symbol is either ported or individually listed.
infrastructure::export # ADR-0011 deferred — state_store::StateStore, RecordStore, DomainRecords, RawRecord, LastError, vector_exporter::VectorExporter, jsonl_exporter. Migration: sub-slice 3.H ports ExportState/Exporter/DomainRecords/RawRecord to domain::exporter, narrows this entry to per-symbol entries, then deletes it. Largest single absorber (14 exclusive sites).
application/asset_download.rs # ADR-0010 inline path absorbed — 1 site: crate::adapters::downloader::Downloader::new. Migration (NOT a precondition): rewrite that call site to build through an injected Arc<dyn DownloaderFactory>. The existence of domain::downloader_factory::DownloaderFactory does NOT satisfy this entry — the site still bypasses the factory. Tracked in #994 sub-slice 3, follow-up to 3.B-1b.
application/crawler/engine.rs # ADR-0010 inline path absorbed — 1 site naming crate::infrastructure::downloader::system_ram_probe::SystemRamProbe that supplies the default ram_probe field in Engine::new. The former session-pool sites (crate::infrastructure::network::session_pool::{DomainSessionPool, SessionPoolConfig}) were repointed to domain::session_port::SessionPort in ADR-0012-B sub-slice 3.F (#1075). Migration: the default probe moves behind a domain-side factory (RamProbePort::system_default) — RAM probe tracked in #1029. The autoscale loop already reads the injected RamProbePort (3.B-1c DONE); only the default construction remains infra-bound. Symbols cited, never line numbers (#1032).
application/elastic_ingestion.rs # ADR-0010 inline path absorbed — 8 non-test sites naming crate::infrastructure::bridge::CpuBridge; the crate::infrastructure::crawler::resource_downloader import is covered by the infrastructure::crawler broad entry. Migration: sub-slice 3.E adds domain::cpu_executor::CpuExecutorPort, 3.E.2 rewrites the ElasticIngestion field to Arc<dyn CpuExecutorPort> and repoints Container wiring. AutotuningConfig sites already moved in #994 sub-slice 1.
