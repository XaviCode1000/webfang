application/container.rs # ADR-0011 DI root — PERMANENT, never removed. The composition root must name concretes (CpuBridge, RayonCpuPool, DomainSessionPool, ResourceDownloader, StateStore, WafInspector). Migration that would end this exemption: make cli/ the only construction site; tracked in #994.
infrastructure::observability # ADR-0011 transversal — PERMANENT, never removed. log_scrape_error and memory_probe carry no domain value; tracing is the stack (no OpenTelemetry since #356). Migration that would end this exemption: none planned.
infrastructure::crawler # ADR-0011 deferred — UrlQueue, robots_utils::RobotsFetcher, SitemapParser, SitemapUrl, SitemapError, SitemapConfig, fetch_url, extract_links. Migration: ADR-0012-B narrows this broad entry to per-symbol entries and deletes it ONE MODULE AT A TIME (cap 22 forbids narrowing crawler+export+observability together — see ADR-0012-B 1.2.2). Do not delete before every symbol is either ported or individually listed.
infrastructure::export # ADR-0011 deferred — state_store::StateStore, RecordStore, DomainRecords, RawRecord, LastError, vector_exporter::VectorExporter, jsonl_exporter. Migration: sub-slice 3.H ports ExportState/Exporter/DomainRecords/RawRecord to domain::exporter, narrows this entry to per-symbol entries, then deletes it. Largest single absorber (14 exclusive sites).
