# Plan: Actualizar AGENTS.md con GitNexus + SDD + Configuración Real

## Descubrimientos del Análisis

| Recurso | Location | Status |
|--------|---------|--------|
| GitNexus skills | `.opencode/skills/gitnexus/` | ✅ Existe (6 skills) |
| GitNexus skills | `.atl/skills/gitnexus/` | ✅ Existe (6 skills) |
| SDD skills | `~/.config/kilo/skills/sdd-*/` | ✅ Existe (11 skills) |
| **rust-skills** | **`.atl/skills/rust-skills/`** | ✅ **Existe (50+ reglas)** |

### rust-skills en `.atl/skills/rust-skills/rules/`
- 50+ reglas en archivos individuales:
  - `mem-*.md`: Memory optimization
  - `perf-*.md`: Performance patterns  
  - `api-*.md`: API design
  - `async-*.md`: Async/await
  - `test-*.md`: Testing
  - `err-*.md`: Error handling
  - `lint-*.md`: Linting
  - `anti-*.md`: Anti-patterns

### AGENTS.md Ya Contiene (líneas 271-452)
- ✅ GitNexus (líneas 271-319): herramientas MCP obligatorias, flujo obligatorio
- ✅ GitNexus detallado (líneas 352-452): debugging, refactoring, impact analysis
- ✅ just pipeline (líneas 76-83): analyze, watch-dev, test-ci
- ✅ Estrategia anti-timeout (líneas 61-96)

### Lo Que FALTA Actualizar
1. **Configuración HDD de nextest** más prominente (actualmente solo en línea 232)
2. **rust-skills** como referencia externa (ubicación correcta)
3. **fff** como herramienta complementaria (no documentada)

---

## Tareas Propuestas

### ✅ TAREA 1: Ampliar configuración HDD
**Actualizar líneas 226-233** (Testing Rules):

```markdown
## Testing Rules

- Write tests for all new functionality
- Tests must be deterministic and isolated
- Mock all external dependencies
- Run `cargo nextest run` (never `cargo test`) before marking any task complete
- Use `--test-threads 2` to avoid HDD I/O bottleneck

### HDD Configuration (CRÍTICO)
**Para Intel i5-4590, 8GB RAM, HDD:**

```toml
# .config/nextest.toml
[profile.default]
threads-required = 2  # MÁXIMO 2 hilos - previene thrashing
retries = { backoff = "exponential", count = 2, delay = "1s" }
slow-timeout = { period = "60s", terminate-after = 3 }
```

- **nunca** usar más de 2 threads en desarrollo
- nextest perfiles: dev (rápido), agent (conservador), ci (completo)
```

### ✅ TAREA 2: Agregar Skills SDD + rust-skills
**Agregar después de línea 319** (después de Self-Check):

```markdown
## SDD Workflow

Este proyecto soporta Spec-Driven Development via skills en `~/.config/kilo/skills/`:

| Skill | Propósito |
|-------|----------|
| sdd-init | Inicializar contexto, detectar stack |
| sdd-explore | Investigar código existente |
| sdd-propose | Crear propuesta |
| sdd-spec | Escribir especificaciones |
| sdd-design | Diseño técnico |
| sdd-tasks | Lista de tareas |
| sdd-apply | Implementar (con gitnexus_impact) |
| sdd-verify | Verificar contra specs |
| sdd-archive | Archivar cambio |

### Pipeline SDD + GitNexus
1. `just analyze` → iniciar
2. `gitnexus_impact` → antes de editar
3. `gitnexus_detect_changes` → pre-commit
4. `just test-ci` → verificación final

## Rust Best Practices

Este proyecto incluye **50+ reglas de rust-skills** en `.atl/skills/rust-skills/rules/`:

| Categoría | Ejemplos |
|-----------|----------|
| Memory | mem-zero-copy, mem-smallvec, mem-compact-string |
| Performance | perf-release-profile, perf-profile-first, perf-collect-once |
| API Design | api-typestate, api-non-exhaustive, api-serde-optional |
| Async | async-tokio-runtime, async-no-lock-await |
| Testing | test-integration-dir, test-tokio-async, test-proptest-properties |
| Error Handling | err-question-mark, err-lowercase-msg |

### Auto-load de rust-skills
Cuando el agente escribe código **Rust**, cargar automáticamente:
```
skill(name: "rust-skills")
```
(Path: `.atl/skills/rust-skills/`)
```

### ✅ TAREA 3: Agregar fff
**Agregar en línea 267** (después de Resources):

```markdown
## Herramientas de Búsqueda

| Herramienta | Uso |
|------------|-----|
| gitnexus_query | Encontrar código por concepto (execution flows) |
| gitnexus_context | Vista 360° de símbolos |
| fff_find_files | Encontrar archivos por nombre |
| fff_grep | Contenido específico |
```

---

## Orden de Ejecución

1. ✅ Task 1: HDD config → agregar en Testing Rules
2. ✅ Task 2: SDD skills → agregar después de GitNexus self-check
3. ✅ Task 3: fff → agregar en Resources

**NO duplicar**: AGENTS.md ya tiene GitNexus completo. Solo AMPLIAR con lo que falta.

---

## Tareas Finales Confirmadas

1. ✅ **Ampliar HDD config** → agregar configuración nextest en Testing Rules
2. ✅ **Agregar SDD + rust-skills** → agregar workflow SDD y 50+ reglas
3. ✅ **Agregar fff** → herramienta complementaria

### Ubicaciones Encontradas
| Skill | Path | Para AGENTS.md |
|-------|------|----------------|
| gitnexus | `.opencode/skills/gitnexus/` | Referencia |
| gitnexus | `.atl/skills/gitnexus/` | Referencia |
| sdd | `~/.config/kilo/skills/sdd-*/` | Referencia |
| **rust-skills** | **`.atl/skills/rust-skills/`** | **REFERENCIAR прямо** |