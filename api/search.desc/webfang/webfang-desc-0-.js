searchState.loadedDescShard("webfang", 0, "WebFang - Production-ready web scraper with Clean …\nBuild the adaptive selector engine when the feature and …\nBuild the AI semantic cleaner and its vault-search ports.\nBuild optional engines and dispatch to the orchestrator.\nCreate FileTraceLayer when –trace-file is present …\nAssemble the vault-search AI ports (#433) from the cleaner…\nRun the unified TUI (or reject it when the <code>ui</code> feature is …\nCheck if running in CI environment.\nParse CLI arguments, translating clap’s …\nPrompt for URL using inquire (interactive mode).\nPrompt for a URL when stdin is a TTY and the <code>ui</code> feature is …\nResolve the webfang config file path (graceful: missing …\nMap verbosity count to tracing levels (0=WARN, 1=INFO, …\nEnsure a URL is present, prompting interactively when …\nRun the unified TUI with collapsible config sections.\nCheck if stdin is a terminal.")