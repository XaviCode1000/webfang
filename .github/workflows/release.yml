name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  # Build Release Binaries
  build-release:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: rust_scraper
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact: rust_scraper
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: rust_scraper.exe

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          target: ${{ matrix.target }}

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Build Release
        run: cargo build --release --target ${{ matrix.target }}

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: rust_scraper-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/${{ matrix.artifact }}

  # Create GitHub Release
  release:
    runs-on: ubuntu-latest
    needs: build-release
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Rename artifacts with version
        run: |
          cd artifacts
          cp rust_scraper-x86_64-unknown-linux-gnu/rust_scraper rust_scraper-x86_64-unknown-linux-gnu/rust_scraper-linux-x86_64
          cp rust_scraper-x86_64-apple-darwin/rust_scraper rust_scraper-x86_64-apple-darwin/rust_scraper-macos-x86_64
          cp rust_scraper-x86_64-pc-windows-msvc/rust_scraper.exe rust_scraper-x86_64-pc-windows-msvc/rust_scraper-windows-x86_64.exe

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          generate_release_notes: true
          files: |
            artifacts/rust_scraper-x86_64-pc-windows-msvc/rust_scraper-windows-x86_64.exe
            artifacts/rust_scraper-x86_64-unknown-linux-gnu/rust_scraper-linux-x86_64
            artifacts/rust_scraper-x86_64-apple-darwin/rust_scraper-macos-x86_64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Publish to crates.io (optional - requires CARGO_REGISTRY_TOKEN secret)
  publish:
    runs-on: ubuntu-latest
    needs: release
    env:
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
    if: ${{ env.CARGO_REGISTRY_TOKEN != '' }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Publish to crates.io
        run: cargo publish --token ${{ secrets.CARGO_REGISTRY_TOKEN }}
